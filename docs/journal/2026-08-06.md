# 2026-08-06

- mercurius round 6: needs_changes, four concerns (one minor — a first) + one advisory. C1 fixed: channel tiers — could-not-issue = harness fault; wire-refused-valid-request = terminal product finding, cancellation-excluded, crash-coalesced; channel pins added.
- round-6 C2 fixed: crashes are terminal and checkpoint-ineligible for their window (boundary-restart exception stands); assertions now the only non-terminal finding class; mid-challenge crash pins added.
- round-6 C3 fixed: rebase on branch — a valid suffix-branch replaces the session's recorded corridor shape with the current gauntlet after the frontier clears, before replay publishes; topology pin gains the branch-then-resume second act.
- round-6 C4 fixed: --world-home is challenge.Main's flag (one owner), Gauntlet.WorldHome the default; applied before lock derivation, engine hands the resolved world to the bootstrap; supersedes round-4 A1 pre-parse; override arm added to the anchor pin.
- round-6 A1 fixed: transport-default pin — no harness-added DL_USE_JSON in child env, parsing succeeds through an ordinary pipe. round 6 fully dispositioned: 4 concerns + 1 advisory, all fixed. starting round 7.
- mercurius round 7: needs_changes, exactly ONE concern, zero advisories — funnel 6/4/5/5/4/4/1. C1 fixed: total verdict — implicit ExpectExit(0)/success-status on unasserted results, displaced by explicit calls; assertion-class on failure; parity evidence flags intentional overrides; default-verdict pins added. strictly stronger than the bash. starting round 8.
- mercurius round 8: needs_changes, two concerns, zero advisories. R8-C1 fixed: instance-scoped crash coalescing — requests carry their fixture's instance identity from world meta; only same-instance evidence merges; two-fixture pin added.
- R8-C2 fixed: the fourth finding class named — "break" (product-surface failure severing dependent flow; terminal, exit 1, below crash, above assertion); census updated to four classes never blurred; model carries the class, renderers name it, decode pin asserts the rendered class. round 8 fully dispositioned. starting round 9.
- mercurius round 9: READY_TO_BUILD — zero concerns, two advisories. A1 taken as configurability + scoped wording (core Bootstrap explicitly shape-agnostic opaque func; reef freshness guarantee scoped to the product binary, SPA only-if-missing; helpers deferred to three-consumer rule); A2 taken (fail-closed pruning arm on the branching-resume pin). handoff self-audit applied: spec status line, WO header (30 concerns/13 advisories, all accepted), decision 4 strictly-before, decision 6 bootstrap-hook, rounds-added-semantics note in the decisions intro. session closing; arc ready for implementation handoff.
- session s_EoXw85LAOTWI closed; synopsis at .mercurius/s_EoXw85LAOTWI/_synopsis.md. arc state: spec + work order converged ready_to_build, both artifacts self-audited for drift. next: michael reviews/commits the planning diff, selects an implementation agent (pipeline: codex default for plan-following work, claude for creative surface), implementation proceeds stage 1 -> 2 -> 3 per the work order. planning phase complete.
